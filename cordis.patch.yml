# dsh-subagent-progress host entry: registers the subagentProgress session
# projection. The client half is discovered via package.json's dsh.client.
- insert:
    - id: subagent-progress
      name: dsh-subagent-progress
